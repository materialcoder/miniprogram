/* components/search/search.wxss */
/* 组件内使用全局样式
  1. 在组件内部创建wxss文件，使用import导入
  2. 在组件内使用externalClasses属性，将外部的样式名称传递到组件里来,（在组件内部不能修改该class名称的样式，只能重新定义一个class名）
  3. 设置组件的属性：styleIsolation: apply-shared
 */
/* @import "iconfont.wxss"; */
.container {
  display: flex;
  align-items: center;
  margin-left: 20rpx;
  position: relative;
}

.bar {
  flex: 1;
  height: 68rpx;
  font-size: 28rpx;
  border-radius: 50rpx;
  background-color: #f5f5f5;
  padding-left: 60rpx;
}

.search {
  background: #d43c33;
  color: #fff;
  margin-left: 20rpx;
  font-size: 24rpx;
}

.in-bar {
  color: #999;
}

.find {
  position: absolute;
  top: 50%;
  transform: translate(15rpx, -35%);
  color: #9f9f9f;
}